apiVersion: v2
name: riksdagskollen
description: Civic tech tool tracking Swedish politicians' promises vs voting records
type: application
version: 0.1.0
appVersion: "0.1.0"

# Database lives on Neon (serverless Postgres). DATABASE_URL is supplied via
# the secret named in values.yaml `dbSecretName`. No in-cluster postgres.
